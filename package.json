{
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "Database",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json-server": "npx json-server db.json --port 4000"
  },
  "author": "Jonathan",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
